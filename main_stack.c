#include "monty.h"
/**
 * main_stack - prints thr top of the stack
 * @head: head of the stack
 * @count: line counter
 * Return: nothing
 */
void main_stack(stack_t **head, unsigned int count)
{
	(void)head;
	(void)count;
	bus.pale = 0;
}
